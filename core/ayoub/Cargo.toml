[workspace]

members = [
    "coiniXerr/src/wallet/",
    "conse",
    "coiniXerr",
    "psychoder",
]


[profile.release]
panic = "abort"
opt-level = "z"
lto = true
codegen-units = 1
overflow-checks = true # panic on any overflow