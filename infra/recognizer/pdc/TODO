


                                            |image and text <-> speech processing|
                                                     |text <-> image|
                                    |natural language processing with BERT & GPT & BART|
                                   |spaCy - stanza - pytorch lightning - plotly - sklearn|
                                          |deep fakee - lip sync - talking head|
                                       |image segmentation (semantic and instance)|
                             |object detection - classification with localization - GAN ops|


📌 fix backpropagation algorithm, the issue with the size of each weights (weights updating process issue for mlp model)
📌 train MLP and CNN params (weights and biases) using GA(ES)
📌 build the confusion matrix (TN, FP, FN and TP) and scores (accuracy, precision, recall and f1 scores) from valid (test) data after training is over
📌 building the prediction bot (complete bot.py script)
📌 create docker services for the bot server using docker-compose file
📌 complete README (link to backward function and docker setup) and
📌 final push 
